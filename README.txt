Kudobuzz

The project is a simple admin panel created using Laravel for the backend and Core UI for the frontend. 
The database used was MySQL(xampp) and is called "kudobuzz".
login credentials for the admin are:
 email = admin@admin.com
 password = password

